import React from 'react';
import { shallow } from 'enzyme';
import PlayerList from './PlayerList';

describe('PlayerList', () => {
    // const playerList = shallow(<PlayerList />);
    //
    // it('renders properly', () => {
    //     expect(playerList).toMatchSnapshot();
    // });
    it('test', () => {
        expect(true).toEqual(true);
    });
});
